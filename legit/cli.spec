# -*- mode: python -*-
a = Analysis(['cli.py', '__init__.py', 'core.py'],
             pathex=['C:\\Users\\akeem.mclennon\\Documents\\projects\\legit\\legit'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='cli.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='cli')
