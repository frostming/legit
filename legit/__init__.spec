# -*- mode: python -*-
a = Analysis(['__init__.py', 'cli.py'],
             pathex=['C:\\Users\\akeem.mclennon\\Documents\\projects\\legit\\legit'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='__init__.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='__init__')
